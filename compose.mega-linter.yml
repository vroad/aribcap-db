services:
  megalinter:
    image: ghcr.io/oxsecurity/megalinter-rust:v9.6.0@sha256:8d3a1ec8bac3dd209e517dc5151720bfaf6c9d2d7d1f1855ee2a40731d2ec563
    # Every variable below can be overridden from the host environment except
    # DEFAULT_WORKSPACE, which must match the volume mount path below.
    environment:
      - APPLY_FIXES
      - ENABLE_LINTERS
      - LOG_LEVEL
      - CLEAR_REPORT_FOLDER=${CLEAR_REPORT_FOLDER:-true}
      - DEFAULT_WORKSPACE=/tmp/lint
    volumes:
      - .:/tmp/lint
